{
  "name": "Expense",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "Expense description"
    },
    "amount": {
      "type": "number",
      "description": "Expense amount in INR"
    },
    "expense_date": {
      "type": "string",
      "format": "date",
      "description": "Date of expense"
    },
    "category": {
      "type": "string",
      "enum": [
        "travel",
        "office_supplies",
        "meals",
        "utilities",
        "rent",
        "salary",
        "marketing",
        "software",
        "maintenance",
        "miscellaneous"
      ],
      "description": "Expense category"
    },
    "payment_mode": {
      "type": "string",
      "enum": [
        "cash",
        "bank_transfer",
        "upi",
        "credit_card",
        "debit_card",
        "cheque"
      ],
      "description": "Mode of payment"
    },
    "bank_account_id": {
      "type": "string",
      "description": "Bank account used for payment"
    },
    "receipt_url": {
      "type": "string",
      "description": "URL to uploaded receipt"
    },
    "approved": {
      "type": "boolean",
      "default": false,
      "description": "Whether expense is approved"
    },
    "approval_status": {
      "type": "string",
      "enum": [
        "not_required",
        "pending",
        "approved",
        "rejected"
      ],
      "default": "not_required",
      "description": "Approval workflow status"
    },
    "approved_by": {
      "type": "string",
      "description": "Email of admin who approved/rejected"
    },
    "approved_by_name": {
      "type": "string",
      "description": "Name of admin who approved/rejected"
    },
    "approved_at": {
      "type": "string",
      "description": "ISO datetime of approval/rejection"
    },
    "rejection_reason": {
      "type": "string",
      "description": "Reason provided when expense is rejected"
    },
    "submitted_by": {
      "type": "string",
      "description": "Email of user who submitted the expense"
    },
    "submitted_by_name": {
      "type": "string",
      "description": "Name of user who submitted the expense"
    },
    "notes": {
      "type": "string",
      "description": "Additional notes"
    },
    "recurrence_type": {
      "type": "string",
      "enum": [
        "none",
        "daily",
        "weekly",
        "monthly",
        "quarterly",
        "custom"
      ],
      "default": "none",
      "description": "Recurrence frequency type"
    },
    "recurrence_interval": {
      "type": "number",
      "default": 1,
      "description": "Interval count for custom recurrence"
    },
    "recurrence_unit": {
      "type": "string",
      "enum": [
        "day",
        "week",
        "month",
        "year"
      ],
      "description": "Unit for custom recurrence interval"
    },
    "recurrence_start_date": {
      "type": "string",
      "format": "date",
      "description": "When recurrence begins"
    },
    "recurrence_end_date": {
      "type": "string",
      "format": "date",
      "description": "When recurrence ends (optional)"
    },
    "last_generated_date": {
      "type": "string",
      "format": "date",
      "description": "Last date up to which instances were generated"
    },
    "parent_expense_id": {
      "type": "string",
      "description": "ID of parent recurring expense template"
    }
  },
  "required": [
    "description",
    "amount",
    "expense_date",
    "category"
  ]
}